apiVersion: v1
kind: Service
metadata:
  name: {{ include "aeroflow.fullname" . }}
  labels:
    {{- include "aeroflow.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
    - port: {{ .Values.service.metricsPort }}
      targetPort: http
      protocol: TCP
      name: metrics
  selector:
    {{- include "aeroflow.selectorLabels" . | nindent 4 }}
